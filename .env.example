# Foci MCP Harness - Secure Public Deployment (Non-Docker)
# Copy to .env and set values

API_KEY=your-super-secret-key-here-change-me
ALLOWED_ORIGINS=https://grok.x.ai,https://yourapp.com
HOST=0.0.0.0
PORT=8000
# Deploy directly with: uvicorn harness.mcp_server:app --host 0.0.0.0 --port $PORT
